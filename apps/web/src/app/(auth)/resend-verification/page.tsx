import { AuthForm } from "@/components/auth/auth-form";

export const metadata = { title: "Verify email — DAIFY" };
export default function ResendVerificationPage() { return <AuthForm mode="resend-verification" />; }
