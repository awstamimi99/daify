services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: daify
      POSTGRES_USER: daify
      POSTGRES_PASSWORD: daify
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U daify -d daify"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - daify-postgres:/var/lib/postgresql/data

  mailpit:
    image: axllent/mailpit:v1.27
    ports:
      - "127.0.0.1:1025:1025"
      - "127.0.0.1:8025:8025"

volumes:
  daify-postgres:
